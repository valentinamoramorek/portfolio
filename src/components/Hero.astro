---
---

<section class="hero px-6 py-6 sm:py-10 md:py-14 text-center">
	<div class="mx-auto max-w-xl sm:max-w-2xl md:max-w-3xl lg:max-w-4xl">
		<p class="hero-eyebrow font-sans text-base sm:text-lg md:text-xl text-foreground mb-2 md:mb-3">
			Hi, I'm Valentina
		</p>

		<h1 class="hero-heading font-heading font-bold text-3xl sm:text-4xl md:text-5xl lg:text-6xl leading-tight text-foreground text-balance">
			B2B Content Manager making brands go from <span class="text-strong-blue">seen</span> to <span class="text-strong-blue">chosen</span>
		</h1>

		<div class="hero-cta mt-5 sm:mt-6 md:mt-8 flex flex-col sm:flex-row justify-center items-center gap-4">
			<a
				href="mailto:valentinamoramorek@gmail.com"
				class="hero-cta-btn inline-block bg-bright-lime text-foreground font-sans font-bold text-base sm:text-lg px-8 sm:px-9 py-3.5 sm:py-4 rounded-2xl hover:brightness-95 transition"
			>
				Let's connect
			</a>
			<a
				href="#work"
				class="hero-cta-btn inline-block bg-transparent border-2 border-strong-blue text-strong-blue font-sans font-bold text-base sm:text-lg px-8 sm:px-9 py-3.5 sm:py-4 rounded-2xl hover:bg-soft-blue transition"
			>
				See my work
			</a>
		</div>
	</div>
</section>

<style>
	@media (max-height: 900px) {
		.hero {
			padding-top: 1rem;
			padding-bottom: 1rem;
		}
		.hero-eyebrow {
			margin-bottom: 0.375rem;
		}
		.hero-heading {
			font-size: clamp(1.5rem, 4vw, 2.5rem);
		}
		.hero-cta {
			margin-top: 0.75rem;
		}
	}

	@media (max-height: 700px) {
		.hero {
			padding-top: 0.5rem;
			padding-bottom: 0.5rem;
		}
		.hero-eyebrow {
			margin-bottom: 0.25rem;
		}
		.hero-heading {
			font-size: clamp(1.25rem, 3.5vw, 1.875rem);
		}
		.hero-cta {
			margin-top: 0.5rem;
			gap: 0.5rem;
		}
		.hero-cta-btn {
			padding-top: 0.5rem;
			padding-bottom: 0.5rem;
		}
	}
</style>
